require "spec_helper"

describe Targetprocess::Ruby do
  it "has a version number" do
    expect(Targetprocess::Ruby::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
